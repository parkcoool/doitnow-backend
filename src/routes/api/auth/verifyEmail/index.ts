import express from "express";

/**
 * @path `/api/auth/verifyEmail`
 * @description 이메일 인증 코드 검증 API입니다.
 */
const verifyEmailRouter = express.Router();

export default verifyEmailRouter;

import "./post";
